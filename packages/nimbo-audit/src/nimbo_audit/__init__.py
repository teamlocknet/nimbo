"""nimbo_audit — CLI de auditoría (codename `nimbo`, slug canónico renombrable).

Capa 3 del DAS (scripts Python desacoplados). Patrones Command + Repository
(ver documento maestro §4.2). Este paquete NO contiene IA (principio de diseño).
"""

__version__ = "0.1.0"
